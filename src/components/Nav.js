import React from "react";
import styled from "styled-components";
const Nav = () => {
  return (
    <NavBar>
      <h2>Hello from nav</h2>
    </NavBar>
  );
};

export default Nav;
NavBar;
const NavBar = styled.nav``;
